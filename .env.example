# CareerAI Environment Configuration
# This application is 100% client-side. No server environment variables are required.
# This file is kept as a reference for future optional integrations only.

# No database required — data is stored in browser (IndexedDB / localStorage)
# No authentication required — local-first architecture
# No API keys required — all intelligence engines run client-side